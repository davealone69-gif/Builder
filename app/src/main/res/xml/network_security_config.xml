<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is denied everywhere EXCEPT loopback, where the local LLM
  endpoints live (Ollama on localhost:11434, local OpenAI-compatible servers
  on 127.0.0.1, and 10.0.2.2 = host machine when running in an emulator).
  All cloud providers (Groq, Hugging Face, OpenRouter, GitHub) are HTTPS.
  Note: a LAN HTTP endpoint (e.g. http://192.168.x.x) needs an HTTPS proxy.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
